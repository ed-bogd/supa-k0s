{{- if .Values.vector.create }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.vector.name }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- toYaml .Values.commonLabels | nindent 4 }}
    app.kubernetes.io/component: logs
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    app: {{ .Values.vector.name }}
  ports:
  - port: {{ .Values.vector.service.port }}
    targetPort: {{ .Values.vector.service.targetPort }}
    name: api
  type: {{ .Values.vector.service.type }}
{{- end }}
